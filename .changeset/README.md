# Changesets

This repo uses Changesets to version and publish the `@fast/*` packages.

- Create a changeset: `npx changeset`
- Version packages: `npx changeset version`
- Publish packages: `npx changeset publish`

CI can run these commands in a release workflow.
